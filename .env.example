# Copy this file to `.env.local` and fill in your values.
# Next.js does NOT read `.env.example` — only `.env.local` is loaded locally.
RESEND_API_KEY=
CONTACT_TO_EMAIL=thiagokm0410@gmail.com
# Use onboarding@resend.dev for testing (Resend sandbox). Replace with your
# verified domain address before production, e.g. noreply@qorifoods.com
CONTACT_FROM_EMAIL="Qori Foods <onboarding@resend.dev>"
